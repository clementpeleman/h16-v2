# react-scroll-rotate@0.0.7 declares `peer react@^16.8.6` and is unmaintained,
# while this project is on React 18. npm 7+ treats that as a hard error in
# `npm ci`, which fails the Docker build even though the package works fine.
# This makes the container build resolve exactly the way local installs do.
legacy-peer-deps=true
