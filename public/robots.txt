# h16.peleman.io is a pre-production host. Blanket disallow so it cannot
# compete with h16.be for the firm's own name, or keep ranking after the move.
#
# WHEN GOING LIVE ON h16.be: replace this whole file with
#   User-agent: *
#   Allow: /
#   Sitemap: https://h16.be/sitemap.xml
# and set NEXT_PUBLIC_ALLOW_INDEXING=true on that deployment.
User-agent: *
Disallow: /
